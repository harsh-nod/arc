arc.module @branching {
  arc.func @select(%cond: i1, %x: i64, %y: i64) -> i64 {
  ^entry:
    arc.cond_br %cond, ^then(%x), ^else(%y)
  ^then(%a: i64):
    arc.br ^exit(%a)
  ^else(%b: i64):
    arc.br ^exit(%b)
  ^exit(%result: i64):
    arc.return %result : i64
  }
}

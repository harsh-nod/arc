arc.module @spawn_await_test {
  arc.func @worker() -> i64 {
  ^entry:
    %r = arc.const 42 : i64
    arc.return %r : i64
  }

  arc.func @main() -> i64 {
  ^entry:
    %task = arc.spawn @worker()
    %val = arc.await %task
    arc.return %val : i64
  }
}

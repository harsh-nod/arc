arc.module @codegen_call {
  arc.func @add(%x: i64, %y: i64) -> i64 {
  ^entry:
    %r = arc.add %x, %y : i64
    arc.return %r : i64
  }
  arc.func @main() -> i64 {
  ^entry:
    %a = arc.const 3 : i64
    %b = arc.const 7 : i64
    %r = arc.call @add(%a, %b)
    arc.return %r : i64
  }
}
